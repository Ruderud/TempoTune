{
  "name": "shared",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/shared/src",
  "projectType": "library",
  "tags": ["type:lib", "scope:shared"],
  "targets": {
    "type-check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @tempo-tune/shared type-check",
        "cwd": "packages/shared"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @tempo-tune/shared lint",
        "cwd": "packages/shared"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @tempo-tune/shared test",
        "cwd": "packages/shared"
      }
    }
  }
}
