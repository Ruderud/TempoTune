# Production web URL
PROD_WEB_URL=https://your-production-url.com

# Dev server port (used with auto-detected IP)
DEV_SERVER_PORT=3000

# Android emulator loopback IP (usually no need to change)
ANDROID_EMULATOR_HOST=10.0.2.2
